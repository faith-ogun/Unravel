# Internal strategy / planning notes (kept private, not for public repo)
metadata/

# Devpost submission drafts (private working copies, not for public repo)
devpost/

# Reference papers (copyrighted whitepapers + generated md/json), kept local
refs/

# Node / Vite
node_modules/
dist/
dist-ssr/
*.local

# Env & secrets
.env
.env.*
!.env.example
*.key
*.pem
service-account*.json

# Editor / OS
.DS_Store
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Staged evidence data (large CSVs, regenerated by scripts and synced via GCS)
backend/_staging/

# Fivetran Connector SDK debug output (local tester warehouse, regenerated)
backend/connectors/*/files/

# Brand source / inspiration assets (the app serves the copies in frontend/public/)
frontend/logo/

# Firebase Hosting deploy cache (local only, regenerated on deploy)
frontend/.firebase/

# Local GIF reference examples (working assets, not for public repo)
frontend/gif examples/

# Private reference material (webinar screenshots, architecture refs), kept local
frontend/Power Your AI Agent with Data - Fivetran and Google Cloud Hackathon Q&A/
agent-architecture-refs/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log
