# Internal strategy / planning notes (kept private, not for public repo)
metadata/

# Devpost submission drafts (private working copies, not for public repo)
devpost/

# Reference papers (copyrighted whitepapers + generated md/json), kept local
refs/

# Node / Vite
node_modules/
dist/
dist-ssr/
*.local

# Env & secrets
.env
.env.*
!.env.example
*.key
*.pem
service-account*.json

# Editor / OS
.DS_Store
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Staged evidence data (large CSVs, regenerated by scripts and synced via GCS)
backend/_staging/

# Brand source / inspiration assets (the app serves the copies in frontend/public/)
frontend/logo/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log
